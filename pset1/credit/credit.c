#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long number = get_long("Input your credit card number: ");
    char s[256];
    sprintf(s, "%ld", number);
    printf("$s", s);
    for (int i = 1; i < 16; i++)
    {
        printf("asdyfyaudyda")
    }
    if (checksum == 0)
    {
        printf("asdyfyaudyda")
    }
}